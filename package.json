{
  "name": "rakutenai",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types example/server.ts",
    "example": "node --experimental-strip-types example/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types example/client.ts"
  },
  "devDependencies": {
    "@ai-sdk/provider": "^4.0.7",
    "@types/node": "^26.2.0"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
